<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <RootNamespace>cloud.charging.open.protocols.EEBUS.SHIP.tests</RootNamespace>
    <AssemblyName>cloud.charging.open.protocols.EEBUS.SHIP.tests</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- FakeTimeProvider: all SHIP timers are driven by a TimeProvider, never by wall clock. -->
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" Version="9.10.0" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageReference Include="NUnit" Version="4.6.1" />
    <PackageReference Include="NUnit3TestAdapter" Version="6.2.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WWCP_EEBUS_SHIP\WWCP_EEBUS_SHIP.csproj" />
  </ItemGroup>

</Project>
